# mori-nest

mori 하네스의 공유 기억 서버. **지금은 코드가 없고 프로토콜 설계 단계다.**

## 하드 룰

1. **스펙 없이 구현 코드를 쓰지 마라.** `docs/design/0001-protocol-requirements.md`는
   요구사항이지 스펙이 아니다. 엔드포인트·스키마를 정한 스펙 문서가 머지되기 전에
   서버 코드를 만들면, 전신이 했던 실수(계약을 정하기 전에 구현이 계약을 결정해버리는
   것)를 반복하는 것이다. 이 단계에서 열리는 이슈는 대부분 **문서 작업**이다.

2. **`docs/inherited/`를 규격으로 읽지 마라.** 전신 프로젝트의 기록이다. 맥락으로만
   쓴다. "inherited에 그렇게 적혀 있다"는 구현 근거가 되지 않는다. mori-nest가 지킬
   것은 `docs/design/`에 다시 적혀야 효력이 있다. 자세한 이유는
   `docs/inherited/_INHERITED.md`.

3. **`shakystar/memorize_hub`을 수정하지 마라.** 그 리포는 실서비스로 떠 있고
   (`memorize-hub-shakystar.fly.dev`), 이 플릿 자신의 기억이 거기 있다. 코드를 읽는
   것은 자유다 (`gh api repos/shakystar/memorize_hub/contents/<path>`). PR·이슈·커밋은
   금지.

4. **설계는 `shakystar/mori`와 엮여 있다.** 프로토콜이 바뀌면 클라이언트도 바뀐다.
   프로토콜에 영향을 주는 결정을 내렸으면 mori
   [#115](https://github.com/shakystar/mori/issues/115)와의 정합성을 확인하고, 어긋나면
   **구현하지 말고 `agent:decision`으로 사람에게 올려라.**

## 이식성 하드 룰 (제어 평면 스토어) — PostgreSQL 이관 예고

[2026-08-10 사람 결정](https://github.com/shakystar/mori-nest/issues/68#issuecomment-5236925659)이
제어 평면 스토어의 목표 엔진을 **PostgreSQL로 명시하되 지금은 옮기지 않는다**고 정했다. 대신
지금부터 지키면 공짜, 나중에 어기면 비싼 하드 룰 넷을 건다. UoW/리포지토리 계층
(mori-nest #130, #131)은 이 넷을 전제로 설계한다.

1. **스토어·리포지토리 인터페이스는 비동기를 유지한다.** 동기 드라이버(`node:sqlite`)라고
   편의상 동기로 바꾸지 않는다. — 근거: 이미 지불된 비용이다. `openControlStore`가 `Promise`를
   반환하고 전 호출부가 `await`로 부르므로, 이관 시 최대 비용이 될 항목이 선제 해결돼 있다.
2. **SQLite 고유 API 의존을 UoW/리포지토리 경계 안에 가둔다** (`lastInsertRowid`가 대표
   사례). — 근거: 경계 밖으로 새면 Postgres에서 `RETURNING`으로 바꿀 자리가 리포 전체로
   흩어진다.
3. **커서·식별자는 이식 가능한 표현만 와이어에 싣는다** — 지금의 십진 정수 문자열을 유지하고
   SQLite 내부값(`AUTOINCREMENT` 정수 등)을 그대로 노출하지 않는다. — 근거: Postgres
   `BIGSERIAL`도 같은 단조 증가 정수를 주므로 **와이어 표현**(십진 문자열)과 클라이언트가 든
   커서의 형식은 이관 후에도 유효하다. 다만 **순서 보장은 따라오지 않는다** — 시퀀스는 발급
   순서와 커밋 가시성 순서가 어긋날 수 있어, SQLite 단일 라이터에서 공짜였던 「발급 순서 =
   가시성 순서」는 이관 시 별도로 세워야 한다 (하드 룰 4와 같은 자리의 문제다).
4. **「단일 라이터라 안전하다」에 기대는 코드를 쓰지 않는다.** 불변식은 응용 코드의 규율이
   아니라 **스토어의 제약**으로 강제한다. — 근거: mori `#189` — *"응용 코드의 «읽고 판정하고
   쓴다»는 조여도 계속 새고, 스토어가 유니크를 강제하면 새지 않는다."* SQLite 단일 라이터가
   지금 봉쇄하는 레이스가 Postgres 이관과 함께 전부 열리므로, 이 규칙이 그 증가를 막는 장치다.

**왜 PostgreSQL인가.** 이 서버는 상업화 전제의 다중 사용자 서비스다. SQLite의 파일=DB 모델이
여기서 최악으로 걸린다 — 테넌트마다 파일을 나누면 크로스 파일 원자성 문제가 테넌트 수만큼
복제되고, 한 파일에 몰면 단일 라이터라 한 고객의 쓰기가 전 고객을 막는다. 거기에
`node:sqlite`가 실험적 API라는 점(`#15`가 이미 리스크로 적었다)이 유료 서비스에서는 감당
불가가 된다.

**왜 지금은 아닌가.** 동시 사용자가 생기기 전까지 Postgres는 아무것도 벌어주지 않으면서
운영 부담과 비용을 먼저 받는다. 그리고 이관 비용이 실측상 낮다(위 하드 룰 1·3이 이미 그
비용의 상당 부분을 선지불했다).

**이관 시 실제로 비쌀 항목 둘** (미리 알고 간다):

- **«`BEGIN`~`COMMIT` 사이 `await` 금지» 불변식의 논증 재구성.** 이관 대상은 제어 평면 쪽이다 —
  `src/control/db.ts`의 `withTransaction`(파일 상단 doc: "`body`가 동기이면 이 구현은 `await`를
  한 번도 하지 않는다")과 `src/control/store.ts:399-400`의 `SqliteControlStore` 클래스 doc
  («트랜잭션 구간을 `await`가 가로지르지 않는다» — 리포지토리 쪽에 남아 있는 같은 불변식).
  둘 다 **동기 드라이버라 끼어들 수 없다**는 사실에 기대므로 `pg`에서는 유지 불가능하고,
  연결 단위 트랜잭션 규율로 **대체**해야 한다 — 기계적 치환이 아니라 논증 재구성이다. (규율의
  출처인 `src/transport/store.ts`는 전송 평면의 이벤트 스토어라 **이관 범위 밖**이다 — 아래
  `#15` 참조.)
- **동시성 재감사.** SQLite 단일 라이터가 지금 봉쇄하고 있는 레이스가 전부 열린다. 하드 룰
  4번이 그 증가를 막는 장치라는 연결을 잊지 않는다.

**`mori-nest#15`의 처리 — supersede가 아니라 유효 기간 명시.** 2026-08-05 결정(이벤트 스토어 =
`node:sqlite`)은 **뒤집지 않는다.** 엔진 선택은 유지하되 «상업 배포 시점에 PostgreSQL로
이관한다»는 예고가 붙는다. 「런타임 의존성 0」도 같은 취급이다 — 규칙으로 유지하되 절대가
아니다. `#15` 자신이 서열을 정해 뒀다: *"실험적 API가 계약을 못 지키는 것보다 의존성 하나가
낫다."* 이관 시점에 이 규칙은 그 서열에 따라 폐기된다. **지금 폐기하지는 않는다** — 아래
「빌드·테스트」 절의 진술은 오늘 사실 그대로다.

## 이 서버가 무엇을 위한 것인지

mori는 개발자 전용 도구가 아니라 **사람들이 협업에 쓰는 하네스**다. 그래서 hub는
백업이 아니라 **유일한 공유 지점**이고, "동료가 내 기억을 언제 보는가"가 곧 제품
품질이다. 설계 판단이 갈릴 때 이 기준으로 정하라.

또 하나: **git이 있는 환경을 가정하지 마라.** 정체성도 동기화도 git 산출물에 의존하면
비개발자 사용자에게 그대로 깨진다.

## 읽는 순서

| 문서 | 내용 |
|---|---|
| `docs/design/0001-protocol-requirements.md` | 새 프로토콜이 만족해야 하는 것 + 계승할 불변식 4개 + 반복하지 말 구현 갭 4개 |
| `docs/inherited/_INHERITED.md` | 전신 기록을 어떻게 읽어야 하는가 |
| `docs/inherited/memorize_hub-SoT/` | 전신 서버의 SoT (H\* 시리즈) |

## 빌드·테스트

Node 22 · TypeScript · pnpm · vitest. 런타임 의존성은 0개다 (`0002 §4.1-3`). **유효 기간**:
상업 배포 시점의 PostgreSQL 이관에서 이 규칙은 서열에 따라 폐기될 수 있다 — 위 「이식성 하드
룰」 절 참조. 오늘은 유효하다.

```bash
pnpm install
pnpm typecheck   # tsc --noEmit (src + test)
pnpm build       # tsc -> dist/
pnpm test        # vitest run
```

CI의 `build-and-test` job(`.github/workflows/ci.yml`)이 위 셋을 그대로 돌린다.
